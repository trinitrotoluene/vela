// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EnemyDespawnFromMobMonitorHandler(ReducerEventContext ctx, ulong enemyEntityId);
        public event EnemyDespawnFromMobMonitorHandler? OnEnemyDespawnFromMobMonitor;

        public void EnemyDespawnFromMobMonitor(ulong enemyEntityId)
        {
            conn.InternalCallReducer(new Reducer.EnemyDespawnFromMobMonitor(enemyEntityId), this.SetCallReducerFlags.EnemyDespawnFromMobMonitorFlags);
        }

        public bool InvokeEnemyDespawnFromMobMonitor(ReducerEventContext ctx, Reducer.EnemyDespawnFromMobMonitor args)
        {
            if (OnEnemyDespawnFromMobMonitor == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEnemyDespawnFromMobMonitor(
                ctx,
                args.EnemyEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EnemyDespawnFromMobMonitor : Reducer, IReducerArgs
        {
            [DataMember(Name = "enemy_entity_id")]
            public ulong EnemyEntityId;

            public EnemyDespawnFromMobMonitor(ulong EnemyEntityId)
            {
                this.EnemyEntityId = EnemyEntityId;
            }

            public EnemyDespawnFromMobMonitor()
            {
            }

            string IReducerArgs.ReducerName => "enemy_despawn_from_mob_monitor";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EnemyDespawnFromMobMonitorFlags;
        public void EnemyDespawnFromMobMonitor(CallReducerFlags flags) => EnemyDespawnFromMobMonitorFlags = flags;
    }
}
