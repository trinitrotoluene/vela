// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void StartQuestChainHandler(ReducerEventContext ctx, int id);
        public event StartQuestChainHandler? OnStartQuestChain;

        public void StartQuestChain(int id)
        {
            conn.InternalCallReducer(new Reducer.StartQuestChain(id), this.SetCallReducerFlags.StartQuestChainFlags);
        }

        public bool InvokeStartQuestChain(ReducerEventContext ctx, Reducer.StartQuestChain args)
        {
            if (OnStartQuestChain == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnStartQuestChain(
                ctx,
                args.Id
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class StartQuestChain : Reducer, IReducerArgs
        {
            [DataMember(Name = "id")]
            public int Id;

            public StartQuestChain(int Id)
            {
                this.Id = Id;
            }

            public StartQuestChain()
            {
            }

            string IReducerArgs.ReducerName => "start_quest_chain";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags StartQuestChainFlags;
        public void StartQuestChain(CallReducerFlags flags) => StartQuestChainFlags = flags;
    }
}
