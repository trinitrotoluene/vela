// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PlayerDuelInitiateHandler(ReducerEventContext ctx, ulong targetPlayerEntityId);
        public event PlayerDuelInitiateHandler? OnPlayerDuelInitiate;

        public void PlayerDuelInitiate(ulong targetPlayerEntityId)
        {
            conn.InternalCallReducer(new Reducer.PlayerDuelInitiate(targetPlayerEntityId), this.SetCallReducerFlags.PlayerDuelInitiateFlags);
        }

        public bool InvokePlayerDuelInitiate(ReducerEventContext ctx, Reducer.PlayerDuelInitiate args)
        {
            if (OnPlayerDuelInitiate == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPlayerDuelInitiate(
                ctx,
                args.TargetPlayerEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PlayerDuelInitiate : Reducer, IReducerArgs
        {
            [DataMember(Name = "target_player_entity_id")]
            public ulong TargetPlayerEntityId;

            public PlayerDuelInitiate(ulong TargetPlayerEntityId)
            {
                this.TargetPlayerEntityId = TargetPlayerEntityId;
            }

            public PlayerDuelInitiate()
            {
            }

            string IReducerArgs.ReducerName => "player_duel_initiate";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PlayerDuelInitiateFlags;
        public void PlayerDuelInitiate(CallReducerFlags flags) => PlayerDuelInitiateFlags = flags;
    }
}
