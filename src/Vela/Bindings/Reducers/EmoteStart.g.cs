// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EmoteStartHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerEmoteRequest request);
        public event EmoteStartHandler? OnEmoteStart;

        public void EmoteStart(SpacetimeDB.Types.PlayerEmoteRequest request)
        {
            conn.InternalCallReducer(new Reducer.EmoteStart(request), this.SetCallReducerFlags.EmoteStartFlags);
        }

        public bool InvokeEmoteStart(ReducerEventContext ctx, Reducer.EmoteStart args)
        {
            if (OnEmoteStart == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEmoteStart(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EmoteStart : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerEmoteRequest Request;

            public EmoteStart(PlayerEmoteRequest Request)
            {
                this.Request = Request;
            }

            public EmoteStart()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "emote_start";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EmoteStartFlags;
        public void EmoteStart(CallReducerFlags flags) => EmoteStartFlags = flags;
    }
}
