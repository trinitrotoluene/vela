// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void TradeCancelServerHandler(ReducerEventContext ctx, ulong sessionEntityId, string resolutionMessage);
        public event TradeCancelServerHandler? OnTradeCancelServer;

        public void TradeCancelServer(ulong sessionEntityId, string resolutionMessage)
        {
            conn.InternalCallReducer(new Reducer.TradeCancelServer(sessionEntityId, resolutionMessage), this.SetCallReducerFlags.TradeCancelServerFlags);
        }

        public bool InvokeTradeCancelServer(ReducerEventContext ctx, Reducer.TradeCancelServer args)
        {
            if (OnTradeCancelServer == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnTradeCancelServer(
                ctx,
                args.SessionEntityId,
                args.ResolutionMessage
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class TradeCancelServer : Reducer, IReducerArgs
        {
            [DataMember(Name = "session_entity_id")]
            public ulong SessionEntityId;
            [DataMember(Name = "resolution_message")]
            public string ResolutionMessage;

            public TradeCancelServer(
                ulong SessionEntityId,
                string ResolutionMessage
            )
            {
                this.SessionEntityId = SessionEntityId;
                this.ResolutionMessage = ResolutionMessage;
            }

            public TradeCancelServer()
            {
                this.ResolutionMessage = "";
            }

            string IReducerArgs.ReducerName => "trade_cancel_server";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags TradeCancelServerFlags;
        public void TradeCancelServer(CallReducerFlags flags) => TradeCancelServerFlags = flags;
    }
}
