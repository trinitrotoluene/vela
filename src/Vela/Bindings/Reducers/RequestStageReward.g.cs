// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void RequestStageRewardHandler(ReducerEventContext ctx, int rewardId);
        public event RequestStageRewardHandler? OnRequestStageReward;

        public void RequestStageReward(int rewardId)
        {
            conn.InternalCallReducer(new Reducer.RequestStageReward(rewardId), this.SetCallReducerFlags.RequestStageRewardFlags);
        }

        public bool InvokeRequestStageReward(ReducerEventContext ctx, Reducer.RequestStageReward args)
        {
            if (OnRequestStageReward == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnRequestStageReward(
                ctx,
                args.RewardId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class RequestStageReward : Reducer, IReducerArgs
        {
            [DataMember(Name = "reward_id")]
            public int RewardId;

            public RequestStageReward(int RewardId)
            {
                this.RewardId = RewardId;
            }

            public RequestStageReward()
            {
            }

            string IReducerArgs.ReducerName => "request_stage_reward";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags RequestStageRewardFlags;
        public void RequestStageReward(CallReducerFlags flags) => RequestStageRewardFlags = flags;
    }
}
