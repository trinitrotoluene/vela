// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PausePlayTimerHandler(ReducerEventContext ctx, PlayerPausePlayTimerRequest request);
        public event PausePlayTimerHandler? OnPausePlayTimer;

        public void PausePlayTimer(PlayerPausePlayTimerRequest request)
        {
            conn.InternalCallReducer(new Reducer.PausePlayTimer(request), this.SetCallReducerFlags.PausePlayTimerFlags);
        }

        public bool InvokePausePlayTimer(ReducerEventContext ctx, Reducer.PausePlayTimer args)
        {
            if (OnPausePlayTimer == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPausePlayTimer(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PausePlayTimer : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerPausePlayTimerRequest Request;

            public PausePlayTimer(PlayerPausePlayTimerRequest Request)
            {
                this.Request = Request;
            }

            public PausePlayTimer()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "pause_play_timer";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PausePlayTimerFlags;
        public void PausePlayTimer(CallReducerFlags flags) => PausePlayTimerFlags = flags;
    }
}
