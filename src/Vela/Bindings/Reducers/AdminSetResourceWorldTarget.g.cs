// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AdminSetResourceWorldTargetHandler(ReducerEventContext ctx, int resourceId, int worldTarget);
        public event AdminSetResourceWorldTargetHandler? OnAdminSetResourceWorldTarget;

        public void AdminSetResourceWorldTarget(int resourceId, int worldTarget)
        {
            conn.InternalCallReducer(new Reducer.AdminSetResourceWorldTarget(resourceId, worldTarget), this.SetCallReducerFlags.AdminSetResourceWorldTargetFlags);
        }

        public bool InvokeAdminSetResourceWorldTarget(ReducerEventContext ctx, Reducer.AdminSetResourceWorldTarget args)
        {
            if (OnAdminSetResourceWorldTarget == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAdminSetResourceWorldTarget(
                ctx,
                args.ResourceId,
                args.WorldTarget
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AdminSetResourceWorldTarget : Reducer, IReducerArgs
        {
            [DataMember(Name = "resource_id")]
            public int ResourceId;
            [DataMember(Name = "world_target")]
            public int WorldTarget;

            public AdminSetResourceWorldTarget(
                int ResourceId,
                int WorldTarget
            )
            {
                this.ResourceId = ResourceId;
                this.WorldTarget = WorldTarget;
            }

            public AdminSetResourceWorldTarget()
            {
            }

            string IReducerArgs.ReducerName => "admin_set_resource_world_target";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AdminSetResourceWorldTargetFlags;
        public void AdminSetResourceWorldTarget(CallReducerFlags flags) => AdminSetResourceWorldTargetFlags = flags;
    }
}
