// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DiscoverEnemyHandler(ReducerEventContext ctx, int enemyId);
        public event DiscoverEnemyHandler? OnDiscoverEnemy;

        public void DiscoverEnemy(int enemyId)
        {
            conn.InternalCallReducer(new Reducer.DiscoverEnemy(enemyId), this.SetCallReducerFlags.DiscoverEnemyFlags);
        }

        public bool InvokeDiscoverEnemy(ReducerEventContext ctx, Reducer.DiscoverEnemy args)
        {
            if (OnDiscoverEnemy == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDiscoverEnemy(
                ctx,
                args.EnemyId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DiscoverEnemy : Reducer, IReducerArgs
        {
            [DataMember(Name = "enemy_id")]
            public int EnemyId;

            public DiscoverEnemy(int EnemyId)
            {
                this.EnemyId = EnemyId;
            }

            public DiscoverEnemy()
            {
            }

            string IReducerArgs.ReducerName => "discover_enemy";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DiscoverEnemyFlags;
        public void DiscoverEnemy(CallReducerFlags flags) => DiscoverEnemyFlags = flags;
    }
}
