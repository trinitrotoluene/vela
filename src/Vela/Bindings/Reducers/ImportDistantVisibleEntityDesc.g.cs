// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ImportDistantVisibleEntityDescHandler(ReducerEventContext ctx, System.Collections.Generic.List<SpacetimeDB.Types.DistantVisibleEntityDesc> records);
        public event ImportDistantVisibleEntityDescHandler? OnImportDistantVisibleEntityDesc;

        public void ImportDistantVisibleEntityDesc(System.Collections.Generic.List<SpacetimeDB.Types.DistantVisibleEntityDesc> records)
        {
            conn.InternalCallReducer(new Reducer.ImportDistantVisibleEntityDesc(records), this.SetCallReducerFlags.ImportDistantVisibleEntityDescFlags);
        }

        public bool InvokeImportDistantVisibleEntityDesc(ReducerEventContext ctx, Reducer.ImportDistantVisibleEntityDesc args)
        {
            if (OnImportDistantVisibleEntityDesc == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnImportDistantVisibleEntityDesc(
                ctx,
                args.Records
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ImportDistantVisibleEntityDesc : Reducer, IReducerArgs
        {
            [DataMember(Name = "records")]
            public System.Collections.Generic.List<DistantVisibleEntityDesc> Records;

            public ImportDistantVisibleEntityDesc(System.Collections.Generic.List<DistantVisibleEntityDesc> Records)
            {
                this.Records = Records;
            }

            public ImportDistantVisibleEntityDesc()
            {
                this.Records = new();
            }

            string IReducerArgs.ReducerName => "import_distant_visible_entity_desc";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ImportDistantVisibleEntityDescFlags;
        public void ImportDistantVisibleEntityDesc(CallReducerFlags flags) => ImportDistantVisibleEntityDescFlags = flags;
    }
}
