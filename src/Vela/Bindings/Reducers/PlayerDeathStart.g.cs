// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PlayerDeathStartHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerDeathTimer timer);
        public event PlayerDeathStartHandler? OnPlayerDeathStart;

        public void PlayerDeathStart(SpacetimeDB.Types.PlayerDeathTimer timer)
        {
            conn.InternalCallReducer(new Reducer.PlayerDeathStart(timer), this.SetCallReducerFlags.PlayerDeathStartFlags);
        }

        public bool InvokePlayerDeathStart(ReducerEventContext ctx, Reducer.PlayerDeathStart args)
        {
            if (OnPlayerDeathStart == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPlayerDeathStart(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PlayerDeathStart : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public PlayerDeathTimer Timer;

            public PlayerDeathStart(PlayerDeathTimer Timer)
            {
                this.Timer = Timer;
            }

            public PlayerDeathStart()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "player_death_start";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PlayerDeathStartFlags;
        public void PlayerDeathStart(CallReducerFlags flags) => PlayerDeathStartFlags = flags;
    }
}
