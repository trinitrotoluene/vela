// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void CheatClaimTotemCompleteCurrentResearchHandler(ReducerEventContext ctx, ulong claimEntityId);
        public event CheatClaimTotemCompleteCurrentResearchHandler? OnCheatClaimTotemCompleteCurrentResearch;

        public void CheatClaimTotemCompleteCurrentResearch(ulong claimEntityId)
        {
            conn.InternalCallReducer(new Reducer.CheatClaimTotemCompleteCurrentResearch(claimEntityId), this.SetCallReducerFlags.CheatClaimTotemCompleteCurrentResearchFlags);
        }

        public bool InvokeCheatClaimTotemCompleteCurrentResearch(ReducerEventContext ctx, Reducer.CheatClaimTotemCompleteCurrentResearch args)
        {
            if (OnCheatClaimTotemCompleteCurrentResearch == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnCheatClaimTotemCompleteCurrentResearch(
                ctx,
                args.ClaimEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class CheatClaimTotemCompleteCurrentResearch : Reducer, IReducerArgs
        {
            [DataMember(Name = "claim_entity_id")]
            public ulong ClaimEntityId;

            public CheatClaimTotemCompleteCurrentResearch(ulong ClaimEntityId)
            {
                this.ClaimEntityId = ClaimEntityId;
            }

            public CheatClaimTotemCompleteCurrentResearch()
            {
            }

            string IReducerArgs.ReducerName => "cheat_claim_totem_complete_current_research";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags CheatClaimTotemCompleteCurrentResearchFlags;
        public void CheatClaimTotemCompleteCurrentResearch(CallReducerFlags flags) => CheatClaimTotemCompleteCurrentResearchFlags = flags;
    }
}
