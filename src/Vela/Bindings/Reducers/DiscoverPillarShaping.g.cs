// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DiscoverPillarShapingHandler(ReducerEventContext ctx, int pillarShapingId);
        public event DiscoverPillarShapingHandler? OnDiscoverPillarShaping;

        public void DiscoverPillarShaping(int pillarShapingId)
        {
            conn.InternalCallReducer(new Reducer.DiscoverPillarShaping(pillarShapingId), this.SetCallReducerFlags.DiscoverPillarShapingFlags);
        }

        public bool InvokeDiscoverPillarShaping(ReducerEventContext ctx, Reducer.DiscoverPillarShaping args)
        {
            if (OnDiscoverPillarShaping == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDiscoverPillarShaping(
                ctx,
                args.PillarShapingId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DiscoverPillarShaping : Reducer, IReducerArgs
        {
            [DataMember(Name = "pillar_shaping_id")]
            public int PillarShapingId;

            public DiscoverPillarShaping(int PillarShapingId)
            {
                this.PillarShapingId = PillarShapingId;
            }

            public DiscoverPillarShaping()
            {
            }

            string IReducerArgs.ReducerName => "discover_pillar_shaping";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DiscoverPillarShapingFlags;
        public void DiscoverPillarShaping(CallReducerFlags flags) => DiscoverPillarShapingFlags = flags;
    }
}
