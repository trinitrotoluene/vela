// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EatHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerEatRequest request);
        public event EatHandler? OnEat;

        public void Eat(SpacetimeDB.Types.PlayerEatRequest request)
        {
            conn.InternalCallReducer(new Reducer.Eat(request), this.SetCallReducerFlags.EatFlags);
        }

        public bool InvokeEat(ReducerEventContext ctx, Reducer.Eat args)
        {
            if (OnEat == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEat(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class Eat : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerEatRequest Request;

            public Eat(PlayerEatRequest Request)
            {
                this.Request = Request;
            }

            public Eat()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "eat";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EatFlags;
        public void Eat(CallReducerFlags flags) => EatFlags = flags;
    }
}
