// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EnemyMoveHandler(ReducerEventContext ctx, SpacetimeDB.Types.EnemyMoveRequest request);
        public event EnemyMoveHandler? OnEnemyMove;

        public void EnemyMove(SpacetimeDB.Types.EnemyMoveRequest request)
        {
            conn.InternalCallReducer(new Reducer.EnemyMove(request), this.SetCallReducerFlags.EnemyMoveFlags);
        }

        public bool InvokeEnemyMove(ReducerEventContext ctx, Reducer.EnemyMove args)
        {
            if (OnEnemyMove == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEnemyMove(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EnemyMove : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public EnemyMoveRequest Request;

            public EnemyMove(EnemyMoveRequest Request)
            {
                this.Request = Request;
            }

            public EnemyMove()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "enemy_move";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EnemyMoveFlags;
        public void EnemyMove(CallReducerFlags flags) => EnemyMoveFlags = flags;
    }
}
