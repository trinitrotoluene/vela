// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AbilityRemoveHandler(ReducerEventContext ctx, byte actionBarIndex, byte localAbilityIndex);
        public event AbilityRemoveHandler? OnAbilityRemove;

        public void AbilityRemove(byte actionBarIndex, byte localAbilityIndex)
        {
            conn.InternalCallReducer(new Reducer.AbilityRemove(actionBarIndex, localAbilityIndex), this.SetCallReducerFlags.AbilityRemoveFlags);
        }

        public bool InvokeAbilityRemove(ReducerEventContext ctx, Reducer.AbilityRemove args)
        {
            if (OnAbilityRemove == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAbilityRemove(
                ctx,
                args.ActionBarIndex,
                args.LocalAbilityIndex
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AbilityRemove : Reducer, IReducerArgs
        {
            [DataMember(Name = "action_bar_index")]
            public byte ActionBarIndex;
            [DataMember(Name = "local_ability_index")]
            public byte LocalAbilityIndex;

            public AbilityRemove(
                byte ActionBarIndex,
                byte LocalAbilityIndex
            )
            {
                this.ActionBarIndex = ActionBarIndex;
                this.LocalAbilityIndex = LocalAbilityIndex;
            }

            public AbilityRemove()
            {
            }

            string IReducerArgs.ReducerName => "ability_remove";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AbilityRemoveFlags;
        public void AbilityRemove(CallReducerFlags flags) => AbilityRemoveFlags = flags;
    }
}
