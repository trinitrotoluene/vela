// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void StagePillarShapingDescHandler(ReducerEventContext ctx, System.Collections.Generic.List<SpacetimeDB.Types.PillarShapingDesc> records);
        public event StagePillarShapingDescHandler? OnStagePillarShapingDesc;

        public void StagePillarShapingDesc(System.Collections.Generic.List<SpacetimeDB.Types.PillarShapingDesc> records)
        {
            conn.InternalCallReducer(new Reducer.StagePillarShapingDesc(records), this.SetCallReducerFlags.StagePillarShapingDescFlags);
        }

        public bool InvokeStagePillarShapingDesc(ReducerEventContext ctx, Reducer.StagePillarShapingDesc args)
        {
            if (OnStagePillarShapingDesc == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnStagePillarShapingDesc(
                ctx,
                args.Records
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class StagePillarShapingDesc : Reducer, IReducerArgs
        {
            [DataMember(Name = "records")]
            public System.Collections.Generic.List<PillarShapingDesc> Records;

            public StagePillarShapingDesc(System.Collections.Generic.List<PillarShapingDesc> Records)
            {
                this.Records = Records;
            }

            public StagePillarShapingDesc()
            {
                this.Records = new();
            }

            string IReducerArgs.ReducerName => "stage_pillar_shaping_desc";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags StagePillarShapingDescFlags;
        public void StagePillarShapingDesc(CallReducerFlags flags) => StagePillarShapingDescFlags = flags;
    }
}
