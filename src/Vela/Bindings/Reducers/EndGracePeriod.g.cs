// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EndGracePeriodHandler(ReducerEventContext ctx, EndGracePeriodTimer timer);
        public event EndGracePeriodHandler? OnEndGracePeriod;

        public void EndGracePeriod(EndGracePeriodTimer timer)
        {
            conn.InternalCallReducer(new Reducer.EndGracePeriod(timer), this.SetCallReducerFlags.EndGracePeriodFlags);
        }

        public bool InvokeEndGracePeriod(ReducerEventContext ctx, Reducer.EndGracePeriod args)
        {
            if (OnEndGracePeriod == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEndGracePeriod(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EndGracePeriod : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public EndGracePeriodTimer Timer;

            public EndGracePeriod(EndGracePeriodTimer Timer)
            {
                this.Timer = Timer;
            }

            public EndGracePeriod()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "end_grace_period";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EndGracePeriodFlags;
        public void EndGracePeriod(CallReducerFlags flags) => EndGracePeriodFlags = flags;
    }
}
