// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void InventorySortHandler(ReducerEventContext ctx, ulong targetEntityId);
        public event InventorySortHandler? OnInventorySort;

        public void InventorySort(ulong targetEntityId)
        {
            conn.InternalCallReducer(new Reducer.InventorySort(targetEntityId), this.SetCallReducerFlags.InventorySortFlags);
        }

        public bool InvokeInventorySort(ReducerEventContext ctx, Reducer.InventorySort args)
        {
            if (OnInventorySort == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnInventorySort(
                ctx,
                args.TargetEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class InventorySort : Reducer, IReducerArgs
        {
            [DataMember(Name = "target_entity_id")]
            public ulong TargetEntityId;

            public InventorySort(ulong TargetEntityId)
            {
                this.TargetEntityId = TargetEntityId;
            }

            public InventorySort()
            {
            }

            string IReducerArgs.ReducerName => "inventory_sort";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags InventorySortFlags;
        public void InventorySort(CallReducerFlags flags) => InventorySortFlags = flags;
    }
}
