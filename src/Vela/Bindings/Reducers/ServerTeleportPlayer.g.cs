// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ServerTeleportPlayerHandler(ReducerEventContext ctx, TeleportPlayerTimer timer);
        public event ServerTeleportPlayerHandler? OnServerTeleportPlayer;

        public void ServerTeleportPlayer(TeleportPlayerTimer timer)
        {
            conn.InternalCallReducer(new Reducer.ServerTeleportPlayer(timer), this.SetCallReducerFlags.ServerTeleportPlayerFlags);
        }

        public bool InvokeServerTeleportPlayer(ReducerEventContext ctx, Reducer.ServerTeleportPlayer args)
        {
            if (OnServerTeleportPlayer == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnServerTeleportPlayer(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ServerTeleportPlayer : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public TeleportPlayerTimer Timer;

            public ServerTeleportPlayer(TeleportPlayerTimer Timer)
            {
                this.Timer = Timer;
            }

            public ServerTeleportPlayer()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "server_teleport_player";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ServerTeleportPlayerFlags;
        public void ServerTeleportPlayer(CallReducerFlags flags) => ServerTeleportPlayerFlags = flags;
    }
}
