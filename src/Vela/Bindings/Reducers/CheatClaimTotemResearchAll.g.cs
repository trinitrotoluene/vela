// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void CheatClaimTotemResearchAllHandler(ReducerEventContext ctx, ulong claimEntityId);
        public event CheatClaimTotemResearchAllHandler? OnCheatClaimTotemResearchAll;

        public void CheatClaimTotemResearchAll(ulong claimEntityId)
        {
            conn.InternalCallReducer(new Reducer.CheatClaimTotemResearchAll(claimEntityId), this.SetCallReducerFlags.CheatClaimTotemResearchAllFlags);
        }

        public bool InvokeCheatClaimTotemResearchAll(ReducerEventContext ctx, Reducer.CheatClaimTotemResearchAll args)
        {
            if (OnCheatClaimTotemResearchAll == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnCheatClaimTotemResearchAll(
                ctx,
                args.ClaimEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class CheatClaimTotemResearchAll : Reducer, IReducerArgs
        {
            [DataMember(Name = "claim_entity_id")]
            public ulong ClaimEntityId;

            public CheatClaimTotemResearchAll(ulong ClaimEntityId)
            {
                this.ClaimEntityId = ClaimEntityId;
            }

            public CheatClaimTotemResearchAll()
            {
            }

            string IReducerArgs.ReducerName => "cheat_claim_totem_research_all";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags CheatClaimTotemResearchAllFlags;
        public void CheatClaimTotemResearchAll(CallReducerFlags flags) => CheatClaimTotemResearchAllFlags = flags;
    }
}
