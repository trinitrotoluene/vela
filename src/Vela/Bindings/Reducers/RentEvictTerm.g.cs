// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void RentEvictTermHandler(ReducerEventContext ctx, RentEvictTimer timer);
        public event RentEvictTermHandler? OnRentEvictTerm;

        public void RentEvictTerm(RentEvictTimer timer)
        {
            conn.InternalCallReducer(new Reducer.RentEvictTerm(timer), this.SetCallReducerFlags.RentEvictTermFlags);
        }

        public bool InvokeRentEvictTerm(ReducerEventContext ctx, Reducer.RentEvictTerm args)
        {
            if (OnRentEvictTerm == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnRentEvictTerm(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class RentEvictTerm : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public RentEvictTimer Timer;

            public RentEvictTerm(RentEvictTimer Timer)
            {
                this.Timer = Timer;
            }

            public RentEvictTerm()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "rent_evict_term";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags RentEvictTermFlags;
        public void RentEvictTerm(CallReducerFlags flags) => RentEvictTermFlags = flags;
    }
}
