// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void BuildingDespawnHandler(ReducerEventContext ctx, BuildingDespawnTimer timer);
        public event BuildingDespawnHandler? OnBuildingDespawn;

        public void BuildingDespawn(BuildingDespawnTimer timer)
        {
            conn.InternalCallReducer(new Reducer.BuildingDespawn(timer), this.SetCallReducerFlags.BuildingDespawnFlags);
        }

        public bool InvokeBuildingDespawn(ReducerEventContext ctx, Reducer.BuildingDespawn args)
        {
            if (OnBuildingDespawn == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnBuildingDespawn(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class BuildingDespawn : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public BuildingDespawnTimer Timer;

            public BuildingDespawn(BuildingDespawnTimer Timer)
            {
                this.Timer = Timer;
            }

            public BuildingDespawn()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "building_despawn";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags BuildingDespawnFlags;
        public void BuildingDespawn(CallReducerFlags flags) => BuildingDespawnFlags = flags;
    }
}
