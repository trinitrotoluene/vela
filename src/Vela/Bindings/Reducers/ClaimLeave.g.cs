// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ClaimLeaveHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerClaimLeaveRequest request);
        public event ClaimLeaveHandler? OnClaimLeave;

        public void ClaimLeave(SpacetimeDB.Types.PlayerClaimLeaveRequest request)
        {
            conn.InternalCallReducer(new Reducer.ClaimLeave(request), this.SetCallReducerFlags.ClaimLeaveFlags);
        }

        public bool InvokeClaimLeave(ReducerEventContext ctx, Reducer.ClaimLeave args)
        {
            if (OnClaimLeave == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnClaimLeave(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ClaimLeave : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerClaimLeaveRequest Request;

            public ClaimLeave(PlayerClaimLeaveRequest Request)
            {
                this.Request = Request;
            }

            public ClaimLeave()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "claim_leave";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ClaimLeaveFlags;
        public void ClaimLeave(CallReducerFlags flags) => ClaimLeaveFlags = flags;
    }
}
