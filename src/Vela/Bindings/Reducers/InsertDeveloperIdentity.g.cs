// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void InsertDeveloperIdentityHandler(ReducerEventContext ctx, string identity, string developerName, string serviceName, string email, bool isExternal);
        public event InsertDeveloperIdentityHandler? OnInsertDeveloperIdentity;

        public void InsertDeveloperIdentity(string identity, string developerName, string serviceName, string email, bool isExternal)
        {
            conn.InternalCallReducer(new Reducer.InsertDeveloperIdentity(identity, developerName, serviceName, email, isExternal), this.SetCallReducerFlags.InsertDeveloperIdentityFlags);
        }

        public bool InvokeInsertDeveloperIdentity(ReducerEventContext ctx, Reducer.InsertDeveloperIdentity args)
        {
            if (OnInsertDeveloperIdentity == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnInsertDeveloperIdentity(
                ctx,
                args.Identity,
                args.DeveloperName,
                args.ServiceName,
                args.Email,
                args.IsExternal
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class InsertDeveloperIdentity : Reducer, IReducerArgs
        {
            [DataMember(Name = "identity")]
            public string Identity;
            [DataMember(Name = "developer_name")]
            public string DeveloperName;
            [DataMember(Name = "service_name")]
            public string ServiceName;
            [DataMember(Name = "email")]
            public string Email;
            [DataMember(Name = "is_external")]
            public bool IsExternal;

            public InsertDeveloperIdentity(
                string Identity,
                string DeveloperName,
                string ServiceName,
                string Email,
                bool IsExternal
            )
            {
                this.Identity = Identity;
                this.DeveloperName = DeveloperName;
                this.ServiceName = ServiceName;
                this.Email = Email;
                this.IsExternal = IsExternal;
            }

            public InsertDeveloperIdentity()
            {
                this.Identity = "";
                this.DeveloperName = "";
                this.ServiceName = "";
                this.Email = "";
            }

            string IReducerArgs.ReducerName => "insert_developer_identity";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags InsertDeveloperIdentityFlags;
        public void InsertDeveloperIdentity(CallReducerFlags flags) => InsertDeveloperIdentityFlags = flags;
    }
}
