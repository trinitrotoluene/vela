// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ItemDropHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerItemDropRequest request);
        public event ItemDropHandler? OnItemDrop;

        public void ItemDrop(SpacetimeDB.Types.PlayerItemDropRequest request)
        {
            conn.InternalCallReducer(new Reducer.ItemDrop(request), this.SetCallReducerFlags.ItemDropFlags);
        }

        public bool InvokeItemDrop(ReducerEventContext ctx, Reducer.ItemDrop args)
        {
            if (OnItemDrop == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnItemDrop(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ItemDrop : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerItemDropRequest Request;

            public ItemDrop(PlayerItemDropRequest Request)
            {
                this.Request = Request;
            }

            public ItemDrop()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "item_drop";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ItemDropFlags;
        public void ItemDrop(CallReducerFlags flags) => ItemDropFlags = flags;
    }
}
