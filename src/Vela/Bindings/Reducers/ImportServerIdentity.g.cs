// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ImportServerIdentityHandler(ReducerEventContext ctx, System.Collections.Generic.List<SpacetimeDB.Types.ServerIdentity> records);
        public event ImportServerIdentityHandler? OnImportServerIdentity;

        public void ImportServerIdentity(System.Collections.Generic.List<SpacetimeDB.Types.ServerIdentity> records)
        {
            conn.InternalCallReducer(new Reducer.ImportServerIdentity(records), this.SetCallReducerFlags.ImportServerIdentityFlags);
        }

        public bool InvokeImportServerIdentity(ReducerEventContext ctx, Reducer.ImportServerIdentity args)
        {
            if (OnImportServerIdentity == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnImportServerIdentity(
                ctx,
                args.Records
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ImportServerIdentity : Reducer, IReducerArgs
        {
            [DataMember(Name = "records")]
            public System.Collections.Generic.List<ServerIdentity> Records;

            public ImportServerIdentity(System.Collections.Generic.List<ServerIdentity> Records)
            {
                this.Records = Records;
            }

            public ImportServerIdentity()
            {
                this.Records = new();
            }

            string IReducerArgs.ReducerName => "import_server_identity";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ImportServerIdentityFlags;
        public void ImportServerIdentity(CallReducerFlags flags) => ImportServerIdentityFlags = flags;
    }
}
