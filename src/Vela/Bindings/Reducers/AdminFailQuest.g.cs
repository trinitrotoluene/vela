// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AdminFailQuestHandler(ReducerEventContext ctx, int questDescId);
        public event AdminFailQuestHandler? OnAdminFailQuest;

        public void AdminFailQuest(int questDescId)
        {
            conn.InternalCallReducer(new Reducer.AdminFailQuest(questDescId), this.SetCallReducerFlags.AdminFailQuestFlags);
        }

        public bool InvokeAdminFailQuest(ReducerEventContext ctx, Reducer.AdminFailQuest args)
        {
            if (OnAdminFailQuest == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAdminFailQuest(
                ctx,
                args.QuestDescId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AdminFailQuest : Reducer, IReducerArgs
        {
            [DataMember(Name = "quest_desc_id")]
            public int QuestDescId;

            public AdminFailQuest(int QuestDescId)
            {
                this.QuestDescId = QuestDescId;
            }

            public AdminFailQuest()
            {
            }

            string IReducerArgs.ReducerName => "admin_fail_quest";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AdminFailQuestFlags;
        public void AdminFailQuest(CallReducerFlags flags) => AdminFailQuestFlags = flags;
    }
}
