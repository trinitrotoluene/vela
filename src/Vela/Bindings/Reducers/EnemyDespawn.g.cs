// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EnemyDespawnHandler(ReducerEventContext ctx, SpacetimeDB.Types.EnemyDespawnTimer timer);
        public event EnemyDespawnHandler? OnEnemyDespawn;

        public void EnemyDespawn(SpacetimeDB.Types.EnemyDespawnTimer timer)
        {
            conn.InternalCallReducer(new Reducer.EnemyDespawn(timer), this.SetCallReducerFlags.EnemyDespawnFlags);
        }

        public bool InvokeEnemyDespawn(ReducerEventContext ctx, Reducer.EnemyDespawn args)
        {
            if (OnEnemyDespawn == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEnemyDespawn(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EnemyDespawn : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public EnemyDespawnTimer Timer;

            public EnemyDespawn(EnemyDespawnTimer Timer)
            {
                this.Timer = Timer;
            }

            public EnemyDespawn()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "enemy_despawn";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EnemyDespawnFlags;
        public void EnemyDespawn(CallReducerFlags flags) => EnemyDespawnFlags = flags;
    }
}
