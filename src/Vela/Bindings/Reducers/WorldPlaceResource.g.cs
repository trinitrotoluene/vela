// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void WorldPlaceResourceHandler(ReducerEventContext ctx, SpacetimeDB.Types.WorldPlaceResourceRequest request);
        public event WorldPlaceResourceHandler? OnWorldPlaceResource;

        public void WorldPlaceResource(SpacetimeDB.Types.WorldPlaceResourceRequest request)
        {
            conn.InternalCallReducer(new Reducer.WorldPlaceResource(request), this.SetCallReducerFlags.WorldPlaceResourceFlags);
        }

        public bool InvokeWorldPlaceResource(ReducerEventContext ctx, Reducer.WorldPlaceResource args)
        {
            if (OnWorldPlaceResource == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnWorldPlaceResource(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class WorldPlaceResource : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public WorldPlaceResourceRequest Request;

            public WorldPlaceResource(WorldPlaceResourceRequest Request)
            {
                this.Request = Request;
            }

            public WorldPlaceResource()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "world_place_resource";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags WorldPlaceResourceFlags;
        public void WorldPlaceResource(CallReducerFlags flags) => WorldPlaceResourceFlags = flags;
    }
}
