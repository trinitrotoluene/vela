// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void TradeAcceptSessionHandler(ReducerEventContext ctx, SpacetimeDB.Types.PlayerTradeAcceptSessionRequest request);
        public event TradeAcceptSessionHandler? OnTradeAcceptSession;

        public void TradeAcceptSession(SpacetimeDB.Types.PlayerTradeAcceptSessionRequest request)
        {
            conn.InternalCallReducer(new Reducer.TradeAcceptSession(request), this.SetCallReducerFlags.TradeAcceptSessionFlags);
        }

        public bool InvokeTradeAcceptSession(ReducerEventContext ctx, Reducer.TradeAcceptSession args)
        {
            if (OnTradeAcceptSession == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnTradeAcceptSession(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class TradeAcceptSession : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public PlayerTradeAcceptSessionRequest Request;

            public TradeAcceptSession(PlayerTradeAcceptSessionRequest Request)
            {
                this.Request = Request;
            }

            public TradeAcceptSession()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "trade_accept_session";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags TradeAcceptSessionFlags;
        public void TradeAcceptSession(CallReducerFlags flags) => TradeAcceptSessionFlags = flags;
    }
}
