// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DeployableMoveOffBoundsHandler(ReducerEventContext ctx, ulong deployableEntityId);
        public event DeployableMoveOffBoundsHandler? OnDeployableMoveOffBounds;

        public void DeployableMoveOffBounds(ulong deployableEntityId)
        {
            conn.InternalCallReducer(new Reducer.DeployableMoveOffBounds(deployableEntityId), this.SetCallReducerFlags.DeployableMoveOffBoundsFlags);
        }

        public bool InvokeDeployableMoveOffBounds(ReducerEventContext ctx, Reducer.DeployableMoveOffBounds args)
        {
            if (OnDeployableMoveOffBounds == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDeployableMoveOffBounds(
                ctx,
                args.DeployableEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DeployableMoveOffBounds : Reducer, IReducerArgs
        {
            [DataMember(Name = "deployable_entity_id")]
            public ulong DeployableEntityId;

            public DeployableMoveOffBounds(ulong DeployableEntityId)
            {
                this.DeployableEntityId = DeployableEntityId;
            }

            public DeployableMoveOffBounds()
            {
            }

            string IReducerArgs.ReducerName => "deployable_move_off_bounds";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DeployableMoveOffBoundsFlags;
        public void DeployableMoveOffBounds(CallReducerFlags flags) => DeployableMoveOffBoundsFlags = flags;
    }
}
