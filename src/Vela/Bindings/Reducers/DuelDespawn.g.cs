// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DuelDespawnHandler(ReducerEventContext ctx, DuelDespawnTimer timer);
        public event DuelDespawnHandler? OnDuelDespawn;

        public void DuelDespawn(DuelDespawnTimer timer)
        {
            conn.InternalCallReducer(new Reducer.DuelDespawn(timer), this.SetCallReducerFlags.DuelDespawnFlags);
        }

        public bool InvokeDuelDespawn(ReducerEventContext ctx, Reducer.DuelDespawn args)
        {
            if (OnDuelDespawn == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDuelDespawn(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DuelDespawn : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public DuelDespawnTimer Timer;

            public DuelDespawn(DuelDespawnTimer Timer)
            {
                this.Timer = Timer;
            }

            public DuelDespawn()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "duel_despawn";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DuelDespawnFlags;
        public void DuelDespawn(CallReducerFlags flags) => DuelDespawnFlags = flags;
    }
}
