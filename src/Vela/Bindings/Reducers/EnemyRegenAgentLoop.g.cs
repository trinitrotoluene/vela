// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void EnemyRegenAgentLoopHandler(ReducerEventContext ctx, EnemyRegenLoopTimer timer);
        public event EnemyRegenAgentLoopHandler? OnEnemyRegenAgentLoop;

        public void EnemyRegenAgentLoop(EnemyRegenLoopTimer timer)
        {
            conn.InternalCallReducer(new Reducer.EnemyRegenAgentLoop(timer), this.SetCallReducerFlags.EnemyRegenAgentLoopFlags);
        }

        public bool InvokeEnemyRegenAgentLoop(ReducerEventContext ctx, Reducer.EnemyRegenAgentLoop args)
        {
            if (OnEnemyRegenAgentLoop == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnEnemyRegenAgentLoop(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class EnemyRegenAgentLoop : Reducer, IReducerArgs
        {
            [DataMember(Name = "_timer")]
            public EnemyRegenLoopTimer Timer;

            public EnemyRegenAgentLoop(EnemyRegenLoopTimer Timer)
            {
                this.Timer = Timer;
            }

            public EnemyRegenAgentLoop()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "enemy_regen_agent_loop";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags EnemyRegenAgentLoopFlags;
        public void EnemyRegenAgentLoop(CallReducerFlags flags) => EnemyRegenAgentLoopFlags = flags;
    }
}
