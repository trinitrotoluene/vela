// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PassiveCraftCancelHandler(ReducerEventContext ctx, ulong passiveCraftEntityId);
        public event PassiveCraftCancelHandler? OnPassiveCraftCancel;

        public void PassiveCraftCancel(ulong passiveCraftEntityId)
        {
            conn.InternalCallReducer(new Reducer.PassiveCraftCancel(passiveCraftEntityId), this.SetCallReducerFlags.PassiveCraftCancelFlags);
        }

        public bool InvokePassiveCraftCancel(ReducerEventContext ctx, Reducer.PassiveCraftCancel args)
        {
            if (OnPassiveCraftCancel == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPassiveCraftCancel(
                ctx,
                args.PassiveCraftEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PassiveCraftCancel : Reducer, IReducerArgs
        {
            [DataMember(Name = "passive_craft_entity_id")]
            public ulong PassiveCraftEntityId;

            public PassiveCraftCancel(ulong PassiveCraftEntityId)
            {
                this.PassiveCraftEntityId = PassiveCraftEntityId;
            }

            public PassiveCraftCancel()
            {
            }

            string IReducerArgs.ReducerName => "passive_craft_cancel";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PassiveCraftCancelFlags;
        public void PassiveCraftCancel(CallReducerFlags flags) => PassiveCraftCancelFlags = flags;
    }
}
