// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AdminCapSkillHandler(ReducerEventContext ctx, int skillId, int level, int newLevel, bool commit);
        public event AdminCapSkillHandler? OnAdminCapSkill;

        public void AdminCapSkill(int skillId, int level, int newLevel, bool commit)
        {
            conn.InternalCallReducer(new Reducer.AdminCapSkill(skillId, level, newLevel, commit), this.SetCallReducerFlags.AdminCapSkillFlags);
        }

        public bool InvokeAdminCapSkill(ReducerEventContext ctx, Reducer.AdminCapSkill args)
        {
            if (OnAdminCapSkill == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAdminCapSkill(
                ctx,
                args.SkillId,
                args.Level,
                args.NewLevel,
                args.Commit
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AdminCapSkill : Reducer, IReducerArgs
        {
            [DataMember(Name = "skill_id")]
            public int SkillId;
            [DataMember(Name = "level")]
            public int Level;
            [DataMember(Name = "new_level")]
            public int NewLevel;
            [DataMember(Name = "commit")]
            public bool Commit;

            public AdminCapSkill(
                int SkillId,
                int Level,
                int NewLevel,
                bool Commit
            )
            {
                this.SkillId = SkillId;
                this.Level = Level;
                this.NewLevel = NewLevel;
                this.Commit = Commit;
            }

            public AdminCapSkill()
            {
            }

            string IReducerArgs.ReducerName => "admin_cap_skill";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AdminCapSkillFlags;
        public void AdminCapSkill(CallReducerFlags flags) => AdminCapSkillFlags = flags;
    }
}
