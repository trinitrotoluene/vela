// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void InitRegionInfoHandler(ReducerEventContext ctx, int worldWidth, int worldHeight, byte regionIndex, byte regionCount);
        public event InitRegionInfoHandler? OnInitRegionInfo;

        public void InitRegionInfo(int worldWidth, int worldHeight, byte regionIndex, byte regionCount)
        {
            conn.InternalCallReducer(new Reducer.InitRegionInfo(worldWidth, worldHeight, regionIndex, regionCount), this.SetCallReducerFlags.InitRegionInfoFlags);
        }

        public bool InvokeInitRegionInfo(ReducerEventContext ctx, Reducer.InitRegionInfo args)
        {
            if (OnInitRegionInfo == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnInitRegionInfo(
                ctx,
                args.WorldWidth,
                args.WorldHeight,
                args.RegionIndex,
                args.RegionCount
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class InitRegionInfo : Reducer, IReducerArgs
        {
            [DataMember(Name = "world_width")]
            public int WorldWidth;
            [DataMember(Name = "world_height")]
            public int WorldHeight;
            [DataMember(Name = "region_index")]
            public byte RegionIndex;
            [DataMember(Name = "region_count")]
            public byte RegionCount;

            public InitRegionInfo(
                int WorldWidth,
                int WorldHeight,
                byte RegionIndex,
                byte RegionCount
            )
            {
                this.WorldWidth = WorldWidth;
                this.WorldHeight = WorldHeight;
                this.RegionIndex = RegionIndex;
                this.RegionCount = RegionCount;
            }

            public InitRegionInfo()
            {
            }

            string IReducerArgs.ReducerName => "init_region_info";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags InitRegionInfoFlags;
        public void InitRegionInfo(CallReducerFlags flags) => InitRegionInfoFlags = flags;
    }
}
