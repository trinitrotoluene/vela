// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AttackHandler(ReducerEventContext ctx, SpacetimeDB.Types.EntityAttackRequest request);
        public event AttackHandler? OnAttack;

        public void Attack(SpacetimeDB.Types.EntityAttackRequest request)
        {
            conn.InternalCallReducer(new Reducer.Attack(request), this.SetCallReducerFlags.AttackFlags);
        }

        public bool InvokeAttack(ReducerEventContext ctx, Reducer.Attack args)
        {
            if (OnAttack == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAttack(
                ctx,
                args.Request
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class Attack : Reducer, IReducerArgs
        {
            [DataMember(Name = "request")]
            public EntityAttackRequest Request;

            public Attack(EntityAttackRequest Request)
            {
                this.Request = Request;
            }

            public Attack()
            {
                this.Request = new();
            }

            string IReducerArgs.ReducerName => "attack";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AttackFlags;
        public void Attack(CallReducerFlags flags) => AttackFlags = flags;
    }
}
