// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DeployableMoveOffClaimHandler(ReducerEventContext ctx, ulong deployableEntityId);
        public event DeployableMoveOffClaimHandler? OnDeployableMoveOffClaim;

        public void DeployableMoveOffClaim(ulong deployableEntityId)
        {
            conn.InternalCallReducer(new Reducer.DeployableMoveOffClaim(deployableEntityId), this.SetCallReducerFlags.DeployableMoveOffClaimFlags);
        }

        public bool InvokeDeployableMoveOffClaim(ReducerEventContext ctx, Reducer.DeployableMoveOffClaim args)
        {
            if (OnDeployableMoveOffClaim == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDeployableMoveOffClaim(
                ctx,
                args.DeployableEntityId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DeployableMoveOffClaim : Reducer, IReducerArgs
        {
            [DataMember(Name = "deployable_entity_id")]
            public ulong DeployableEntityId;

            public DeployableMoveOffClaim(ulong DeployableEntityId)
            {
                this.DeployableEntityId = DeployableEntityId;
            }

            public DeployableMoveOffClaim()
            {
            }

            string IReducerArgs.ReducerName => "deployable_move_off_claim";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DeployableMoveOffClaimFlags;
        public void DeployableMoveOffClaim(CallReducerFlags flags) => DeployableMoveOffClaimFlags = flags;
    }
}
