// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void CompleteQuestChainHandler(ReducerEventContext ctx, int id);
        public event CompleteQuestChainHandler? OnCompleteQuestChain;

        public void CompleteQuestChain(int id)
        {
            conn.InternalCallReducer(new Reducer.CompleteQuestChain(id), this.SetCallReducerFlags.CompleteQuestChainFlags);
        }

        public bool InvokeCompleteQuestChain(ReducerEventContext ctx, Reducer.CompleteQuestChain args)
        {
            if (OnCompleteQuestChain == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnCompleteQuestChain(
                ctx,
                args.Id
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class CompleteQuestChain : Reducer, IReducerArgs
        {
            [DataMember(Name = "id")]
            public int Id;

            public CompleteQuestChain(int Id)
            {
                this.Id = Id;
            }

            public CompleteQuestChain()
            {
            }

            string IReducerArgs.ReducerName => "complete_quest_chain";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags CompleteQuestChainFlags;
        public void CompleteQuestChain(CallReducerFlags flags) => CompleteQuestChainFlags = flags;
    }
}
