// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void DiscoverAchievementHandler(ReducerEventContext ctx, int achievementId);
        public event DiscoverAchievementHandler? OnDiscoverAchievement;

        public void DiscoverAchievement(int achievementId)
        {
            conn.InternalCallReducer(new Reducer.DiscoverAchievement(achievementId), this.SetCallReducerFlags.DiscoverAchievementFlags);
        }

        public bool InvokeDiscoverAchievement(ReducerEventContext ctx, Reducer.DiscoverAchievement args)
        {
            if (OnDiscoverAchievement == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnDiscoverAchievement(
                ctx,
                args.AchievementId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class DiscoverAchievement : Reducer, IReducerArgs
        {
            [DataMember(Name = "achievement_id")]
            public int AchievementId;

            public DiscoverAchievement(int AchievementId)
            {
                this.AchievementId = AchievementId;
            }

            public DiscoverAchievement()
            {
            }

            string IReducerArgs.ReducerName => "discover_achievement";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags DiscoverAchievementFlags;
        public void DiscoverAchievement(CallReducerFlags flags) => DiscoverAchievementFlags = flags;
    }
}
