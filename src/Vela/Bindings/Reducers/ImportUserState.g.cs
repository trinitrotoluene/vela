// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ImportUserStateHandler(ReducerEventContext ctx, System.Collections.Generic.List<SpacetimeDB.Types.UserState> records);
        public event ImportUserStateHandler? OnImportUserState;

        public void ImportUserState(System.Collections.Generic.List<SpacetimeDB.Types.UserState> records)
        {
            conn.InternalCallReducer(new Reducer.ImportUserState(records), this.SetCallReducerFlags.ImportUserStateFlags);
        }

        public bool InvokeImportUserState(ReducerEventContext ctx, Reducer.ImportUserState args)
        {
            if (OnImportUserState == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnImportUserState(
                ctx,
                args.Records
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ImportUserState : Reducer, IReducerArgs
        {
            [DataMember(Name = "records")]
            public System.Collections.Generic.List<UserState> Records;

            public ImportUserState(System.Collections.Generic.List<UserState> Records)
            {
                this.Records = Records;
            }

            public ImportUserState()
            {
                this.Records = new();
            }

            string IReducerArgs.ReducerName => "import_user_state";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ImportUserStateFlags;
        public void ImportUserState(CallReducerFlags flags) => ImportUserStateFlags = flags;
    }
}
