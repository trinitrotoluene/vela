// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void AttackScheduledHandler(ReducerEventContext ctx, AttackTimer timer);
        public event AttackScheduledHandler? OnAttackScheduled;

        public void AttackScheduled(AttackTimer timer)
        {
            conn.InternalCallReducer(new Reducer.AttackScheduled(timer), this.SetCallReducerFlags.AttackScheduledFlags);
        }

        public bool InvokeAttackScheduled(ReducerEventContext ctx, Reducer.AttackScheduled args)
        {
            if (OnAttackScheduled == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnAttackScheduled(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class AttackScheduled : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public AttackTimer Timer;

            public AttackScheduled(AttackTimer Timer)
            {
                this.Timer = Timer;
            }

            public AttackScheduled()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "attack_scheduled";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags AttackScheduledFlags;
        public void AttackScheduled(CallReducerFlags flags) => AttackScheduledFlags = flags;
    }
}
