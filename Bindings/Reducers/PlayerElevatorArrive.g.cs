// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PlayerElevatorArriveHandler(ReducerEventContext ctx, PlayerUseElevatorTimer timer);
        public event PlayerElevatorArriveHandler? OnPlayerElevatorArrive;

        public void PlayerElevatorArrive(PlayerUseElevatorTimer timer)
        {
            conn.InternalCallReducer(new Reducer.PlayerElevatorArrive(timer), this.SetCallReducerFlags.PlayerElevatorArriveFlags);
        }

        public bool InvokePlayerElevatorArrive(ReducerEventContext ctx, Reducer.PlayerElevatorArrive args)
        {
            if (OnPlayerElevatorArrive == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPlayerElevatorArrive(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PlayerElevatorArrive : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public PlayerUseElevatorTimer Timer;

            public PlayerElevatorArrive(PlayerUseElevatorTimer Timer)
            {
                this.Timer = Timer;
            }

            public PlayerElevatorArrive()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "player_elevator_arrive";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags PlayerElevatorArriveFlags;
        public void PlayerElevatorArrive(CallReducerFlags flags) => PlayerElevatorArriveFlags = flags;
    }
}
