// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void ResourceSpawnScheduledHandler(ReducerEventContext ctx, ResourceSpawnTimer timer);
        public event ResourceSpawnScheduledHandler? OnResourceSpawnScheduled;

        public void ResourceSpawnScheduled(ResourceSpawnTimer timer)
        {
            conn.InternalCallReducer(new Reducer.ResourceSpawnScheduled(timer), this.SetCallReducerFlags.ResourceSpawnScheduledFlags);
        }

        public bool InvokeResourceSpawnScheduled(ReducerEventContext ctx, Reducer.ResourceSpawnScheduled args)
        {
            if (OnResourceSpawnScheduled == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnResourceSpawnScheduled(
                ctx,
                args.Timer
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class ResourceSpawnScheduled : Reducer, IReducerArgs
        {
            [DataMember(Name = "timer")]
            public ResourceSpawnTimer Timer;

            public ResourceSpawnScheduled(ResourceSpawnTimer Timer)
            {
                this.Timer = Timer;
            }

            public ResourceSpawnScheduled()
            {
                this.Timer = new();
            }

            string IReducerArgs.ReducerName => "resource_spawn_scheduled";
        }
    }

    public sealed partial class SetReducerFlags
    {
        internal CallReducerFlags ResourceSpawnScheduledFlags;
        public void ResourceSpawnScheduled(CallReducerFlags flags) => ResourceSpawnScheduledFlags = flags;
    }
}
